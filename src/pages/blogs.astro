---
import BotonSecundario from "../components/Common/BotonSecundario.astro";
import Nav from "../components/Common/Nav.astro";
import BotonBlog from "../components/PaginaDeBlogs/BotonBlog.astro";
import PaginaDeBlogs from "../layouts/PaginaDeBlogs.astro";
import { getCollection } from "astro:content";

const blogs = await getCollection("blog");
---

<PaginaDeBlogs>
  <Nav />
  <main
    class="flex flex-col items-center mt-28 mb-24 px-4 h-[100vh]"
  >
    <div
      class="max-w-[900px] w-full flex flex-col justify-center items-center gap-8"
    >
      <h1 class="reveal">Blogs</h1>
      <div class="w-full mt-8">
        <ul class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 w-full">
          {blogs.map((blog, i) => (
            <li class="flex h-full reveal" data-reveal-delay={100 + i * 100}>
              <BotonBlog texto={blog.data.title} enlace={`/blog/${blog.id}`} />
            </li>
          ))}
        </ul>
      </div>
    </div>
  </main>
</PaginaDeBlogs>
